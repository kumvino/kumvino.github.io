# To enable local development of the website, follow the steps below
# 1. $ sudo apt-get install ruby-dev
# 2. $ bundle install
# 3. $ bundle exec jekyll serve
#
# Step 1 allows certain dependencies to be installed from the bundle
# Step 2 installs them
# Step 3 enables running of the page in a local environment

source 'https://rubygems.org'
gem 'github-pages', group: :jekyll_plugins
